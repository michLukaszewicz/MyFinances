services:
  - type: web
    name: myfinances-api
    runtime: docker
    dockerfilePath: MyFinances/backend/Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: ConnectionStrings__Default
        sync: false
      - key: Auth__AllowedEmail
        sync: false
